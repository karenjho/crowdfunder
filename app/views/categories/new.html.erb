<h1>Create A New Category</h1>

<%= form_for @category do |f| %>
  <div class="field">
    <%= f.label :name, "Category Name" %>
    <%= f.text_field :name %>
  </div>

  <div class="action">
    <%= f.submit "Submit Category" %>
  </div>
  <% end %>

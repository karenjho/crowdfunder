<div id="pledge-form">
  <%= form_for ([@reward, @pledge]) do |f| %>
    <div class="field">
      <%= f.label :amount %><br>
      <%= f.number_field(:amount, min: 1.00, step: 1.00) %>
    </div>
    <div class="action">
      <%= f.submit "Submit Pledge" %>
    </div>
  <% end %>
</div>
